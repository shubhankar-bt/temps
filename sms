"message": "123456 is the OTP to be used for setting your temporary password for CRS application in SBI. This OTP is valid for 10 minutes.",


i need this message to be formatted in tyhe format where i would replace this otp and 10 expiry minutes with variables.

i am using the below code to replace the data :

 Template compiledTemplate = templateCacheService.getCompiledTemplate(templateCode, channel, language);

        // 2. Merge dynamic data (OTP, Names) into the template
        try (StringWriter writer = new StringWriter()) {
            compiledTemplate.process(templateData, writer);


give the exact format
